#!/usr/bin/env bash

set -e
[ -n "$RBENV_DEBUG" ] && set -x

if [ "$1" = "--version" ] || [ "$1" = "-v" ]; then
  echo "rbenv-mjit 0.1.0"
  exit
fi

if [[ $RBENV_VERSION = "2."[67]* ]]; then
  echo "export RUBYOPT=--jit"
fi
